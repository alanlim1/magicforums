<div class="topics text-center">
	<div class="featurestopics">
		<div class="featuretopics">
			<h1><%= @post.title %></h1>
			<p><%= @post.description %></p>
			<%= link_to "Delete", post_path(@post), method: :delete, data: { confirm: "Are you sure?" } %>
		</div>
	</div>	
</div>